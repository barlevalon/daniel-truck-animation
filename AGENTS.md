## Build & Run

- Serve site: `python -m http.server 8000 --directory public`

## Validation

Run these after implementing to get immediate feedback:

- Screenshot flow: `node test-automation/screenshot.js full-test`

## Operational Notes

- Scoped planning: `./loop.sh plan-work "short description"`
- Loop does not auto-push; push manually when ready.
- If scoped plan is wrong, rerun plan-work to regenerate.

## Codebase Patterns

Document any shared utilities or architectural patterns to preserve.
